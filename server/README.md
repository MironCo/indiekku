# Server Build Directory

Place your Unity server build files in this directory.

Indiekku will automatically detect and run your server executable.

## Supported formats:
- Linux builds (`.x86_64`)
- Windows builds (`.exe`)

The first matching executable found will be used.